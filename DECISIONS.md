# 判断待ちリスト

1.0 に向けて自律的に作業を進めるための、人間への問い合わせキュー。
**このファイルに直接追記して回答してください。** 次の作業の開始時に読みます。

回答は各項目の `**回答:**` 行に書いてください。書式は自由です。

---

## 運用ルール

### 止まる条件（この3つだけ）

1. **取り返しがつかない / 外向き** — 公開、送信、鍵の生成、外部サービスへの登録
2. **誤ると金銭または安全に影響する** — 事業者プレフィックスの値、緊急通報の扱い
3. **あなたにしか実行できない** — 実機確認、アカウント操作、秘密情報

### 止まらない条件

上記以外はすべて **既定値を決めて進み**、このファイルに記録する。
記録した既定値は後から覆せるよう、次を守る:

- 1 スライス = 1 コミット（`git revert` で単体で戻せる）
- 既定値の選択は 1 ファイルに閉じ込める（横断的に散らさない）
- 挙動はテストで固定する（覆したときに何が壊れるかが即座に分かる）

つまり「迷ったら止まる」ではなく「**迷ったら安く覆せる形で進む**」。
待ち時間をゼロにする代わりに、やり直しコストを小さく保つ方に投資する。

### 安全性に関する例外

`ProtectedNumbers`（緊急通報・特番の保護）に関わる変更は、既定値で進めない。
必ずこのファイルに起票して回答を待つ。

---

## 🔴 停止中（回答が来るまで進めない）

### D-01. リリース用 keystore

署名鍵の作成にはパスワードが要る。秘密情報なので私は生成も保管もしない。

やっていただくこと（1.0 の最終段階で構いません）:
```
keytool -genkeypair -v -keystore release.keystore -alias prefixdialer \
        -keyalg RSA -keysize 4096 -validity 10000
```
生成後、`keystore.properties`（`.gitignore` 済み）に配置してください。
`build.gradle.kts` 側の `signingConfigs` は先に用意しておきます。

**回答:** 完了（2026-08-26）。RSA 4096 / alias `prefixdialer` で作成済み。
署名済みの `app-release.apk` と `app-release.aab` のビルドを確認した
（`apksigner verify` で v2 署名を確認、証明書 CN=Tmlk）。

⚠️ **keystore と keystore.properties は必ずバックアップしてください。**
紛失すると Play にアップロード済みのアプリを二度と更新できなくなります。
リポジトリには含まれないので、別の安全な場所に控えが要ります。

### D-02. プリセットの追加

現在の収録は G-Call（あなたの実機確認済み）のみ。
他社を追加するには、実際の契約と発信明細での確認が要る。**推測では絶対に追加しない。**

追加したい事業者があれば、以下を書いてください:
- 事業者名 /  携帯向けのプレフィックスと先頭0の扱い / 固定向けのプレフィックスと先頭0の扱い
- 確認方法（公式資料 / 実機発信）

**回答:**
楽天でんわ 003768 https://denwa.rakuten.co.jp/about.html
まずはこれがあるといいとおもいます

**対応（2026-08-26）:** 収録済み。全種別とも `003768` + 先頭 0 を残す形式。

公式ページはプレフィックスの値しか書いておらず、**先頭 0 の扱いが明記されて
いなかった**ため追加で裏を取った。ITmedia Mobile の実機記事（2016-03-17）に
「`090-8＊＊＊-＊＊＊＊` をダイヤルすると `0037-68-` が頭についた状態で発信される」
という具体例があり、先頭 0 を残す形式（G-Call と同じ）と確認できた。

実機での発信・明細確認は未実施 → H-06 として起票。

### D-19. GitHub 公開前のコミット著者メールアドレス ★公開後は手遅れ

公開リポジトリのコミット著者メールは誰でも API で取得でき、収集ボットの対象になる。
公開後に履歴を書き換えても、フォークやキャッシュに残るため取り返しがつかない。

**回答:** 書き換える。

**対応（2026-08-26）:** 全 19 コミットの著者・コミッタを GitHub の noreply
（`<ID>+tmlksu@users.noreply.github.com`）へ書き換え済み。ローカルの
`user.email` も同じ値に設定したので、以降のコミットも自動的にこの形式になる。

書き換え前に `git bundle` でバックアップを取得している。
GitHub 上のリポジトリは書き換え時点で非公開だったため、旧アドレスは未露出。

⚠️ **GitHub 側（`git@github.com:tmlksu/prefix-dialer.git`）には書き換え前の
初回コミットがまだ残っている。** ローカルからは完全に消したが、リモートは別。

force push で置き換えても、GitHub は到達不能になった古いコミットを SHA 指定で
しばらく参照できる状態に残す。実用上は SHA を知らないと辿れないが、確実を期すなら
**GitHub 上のリポジトリを一度削除して作り直す**のが最も確実。
現在 push 済みなのは初回コミット 1 つだけなので、失うものはない。

- [x] リポジトリを作り直した（2026-08-26）
- [x] public へ切り替えた（2026-08-26）

**結果:** 旧コミット `1fad370` は参照不可になったことを認証あり・匿名の両方で確認済み
（GitHub は 404 ではなく HTTP 422 "No commit found for SHA" を返す）。
公開後のリポジトリ上の全コミットは noreply アドレスのみ。

### D-20. GitHub 配布 APK と Play 版の署名鍵 ★あとで揃えられない

GitHub で自前の鍵で署名した APK を配ったあと、Play にも出す場合の話。

Play は新規アプリに Play App Signing を必須にしている。既定では Google が生成した
鍵で署名されるため、**GitHub 版と Play 版の署名が食い違う**。署名が違うアプリは
上書き更新できないので、**GitHub 版を入れたユーザーは一度アンインストールしないと
Play 版に移行できない**（設定も消える）。

回避策は、Play App Signing に**今作った `release.keystore` を「アプリ署名鍵」として
アップロードする**こと。そうすれば Play 版も同じ鍵で署名され、両者が互換になる。
ただしこれは**アプリの初回登録時にしか選べない**。後から差し替えられない。

- **A. GitHub 配布のみで当面いく** — 今は何も気にしなくてよい
- **B. Play も視野に入れる**（推奨）— 初回登録時に既存の鍵をアップロードする。
  それまで `release.keystore` を絶対に紛失しないこと

**回答:** B。あとで Play 配布する。署名をあえて変える理由がないので、
`release.keystore` をアプリ署名鍵としてアップロードする。

**これに伴う制約:**
- Play への初回登録時に「既存のアプリ署名鍵をアップロード」を選ぶこと。
  **後から変更できない。** 既定のまま進めると Google 生成の鍵になり、
  GitHub 版と Play 版が上書き更新できなくなる
- それまで `release.keystore` と `keystore.properties` を紛失しないこと

**補足（2026-09-18）:** Play の現行ヘルプでは、アプリ署名鍵の選択は「初回登録時のみ」ではなく「**オープンテストまたは製品版トラックにリリースを公開するまで**」変更できると書かれている（PLAY-RELEASE.md §3）。ただし個人アカウントは 14 日間のクローズドテストを挟むため、うっかり越えないよう**アプリ作成直後に済ませる**運用は変えない。

### D-03. Play Console への提出

外向きの操作なので、私からは実行しません。提出直前に必ず確認します。
プライバシーポリシーの本文は私が用意しますが、**公開先（URL）はあなたの管理下**です。

**回答:**
了解です

**現状（2026-08-26）:** Play への提出は未実施。GitHub Releases での配布を先行した。
提出時は D-20 のとおり、初回登録で `release.keystore` をアプリ署名鍵としてアップロードすること。
プライバシーポリシーは `PRIVACY.md` を公開済みなので、その URL を登録すればよい。

---

## 🟡 既定値で進行中（回答があれば差し替えます）

回答が無ければこのまま進みます。後から覆せます。

### D-10. applicationId

**既定値:** `io.github.tmlksu.prefixdialer`
所有ドメインがあればそちらを優先します。**一度 Play に公開すると変更不可**なので、
リリース前に必ず確定させてください（それまでは何度でも変えられます）。

**回答:** 個人的にはこれでいいと思ってるんですが、おすすめのapplicationIDがありますか？それかドメインを自分で取るかどうか、ということなんですかね
・

**決定（2026-09-19）:** 現状維持。`io.github.<ユーザー名>` は GitHub 上のプロジェクトの慣例で、
Play でもそのまま通る。独自ドメインはブランド名を別に立てたい場合にだけ意味があり、今は不要。
1.0.0 を GitHub でこの ID で配布済みのため、変えると既存ユーザーが上書き更新できなくなる（D-20 の前提も崩れる）。
Play Console のパッケージ名欄にはこの値を入れる。公開後は変更不可。

### D-11. 設定の保存先

**既定値:** `SharedPreferences`
DataStore は依存が増える割に、この規模（ルールセット1つ + フラグ数個）では利点が薄い。

**回答:**

### D-12. ローミング中の挙動

**既定値:** 自動的にプレフィックスを停止する。デフォルト有効、設定で解除可。
海外でプレフィックスを付けても無意味で、課金だけが変わる可能性があるため安全側に倒す。

**回答:** いいですね、ローミング中は停止するべきです。

### D-13. SIM ごとの適用の既定値

**既定値:** 全 SIM で有効。設定画面で SIM ごとに個別 OFF にできる。
「デフォルト全 OFF で明示的に有効化」も考えたが、現行の挙動からの後退になるため見送り。

**回答:** よい。

### D-14. 履歴書き換え（CALL_LOG）の既定値

**既定値:** デフォルト **OFF**。設定で有効化した瞬間に権限を要求する。
インストール直後に通話履歴の権限を求めない形にする（Play ポリシーとユーザー保護の両面）。
※ あなたの現在の使い方では ON が前提なので、初回起動時に一度だけ案内を出します。

**回答:** よい。

### D-15. 発信記録の保持

**既定値:** 直近 100 件、アプリ内のみ、端末外に送信しない。設定から全消去可能。

**回答:** よい

### D-16. 多言語

**既定値:** 日本語 + 英語。
機能が日本の番号計画に強く依存しているため、それ以上は広げない。

**回答:** そうですね、いいですね。

### D-17. アプリアイコン

**既定値:** 単色ベースの簡素なアダプティブアイコンを私が用意する。
デザインの好みがあれば差し替えてください（画像を置いていただければ組み込みます）。

**回答:** アプリアイコン、もう少し画像部分を小さくしてください。Galaxyのアイコンの枠からはみ出てます

**対応（2026-08-26）:** アダプティブアイコンの安全領域（中央 66dp の円）に収まるよう
縮小した。実機で確認いただき OK。

---

## 🔵 あなたにしかできない作業

コードが完成しても、これらが済むまで 1.0 は出せません。
各スライスの完了時に、確認してほしい項目をここに追記していきます。

### H-01. 実機での発信確認

**緊急通報番号には絶対に発信しないでください。**
`110` / `118` / `119` / `112` / `#7119` / `#8000` / `#9110` / `#8103` は対象外です。
確認に使えるのは `117`（時報）程度で、これも必要最小限に。

- [ ] 通常の携帯番号への発信でプレフィックスが付く
- [ ] 履歴が元番号へ戻る（詳細設定で「通話履歴の書き換え」を有効にしたうえで）
- [ ] 設定 UI で変更した内容が次の発信に反映される
- [ ] マスタースイッチ OFF で素通しになる
- [ ] 発信記録に結果と理由が残る
- [ ] 設定のエクスポート/インポートが往復する

### H-05. 難読化した release APK の動作 ★重要

R8 を有効にしたため、debug では出ない問題が release だけで起きうる。
特に libphonenumber はメタデータをリフレクションで読むので、keep ルールが
足りないと**実行時に番号の判定が丸ごと失敗する**。ビルドは通っているが実機は未確認。

```
./gradlew assembleRelease   # keystore.properties があれば署名される
```

- [x] 通常の発信にプレフィックスが付く（2026-08-26 実機確認）
- [x] **release APK で** 通常の発信にプレフィックスが付く（2026-08-26 実機確認）
- [x] release APK で設定画面が正常に表示・保存される（2026-08-26 実機確認）

**結論: R8 難読化後も問題なく動作する。** keep ルールは十分だった。

**2026-08-26:** release APK のインストールが「インストールされませんでした」で失敗。
原因は R8 ではなく署名鍵の違いだった。debug 版と release 版は applicationId が同じで
署名が異なるため、Android が上書き更新を拒否する
（`INSTALL_FAILED_UPDATE_INCOMPATIBLE`）。debug 版をアンインストールしてから入れる。
配布後にユーザーも踏む問題なので README と CHANGELOG に記載した。

**静的検証（2026-08-26）:** release APK を展開し、R8 通過後も
`PhoneNumberMetadataProto_JP` / `ShortNumberMetadataProto_JP` が残っていること、
libphonenumber のクラスと 2 つの Service が難読化されずに保持されていることを確認した。
keep ルールは効いている。

### H-06. 楽天でんわプリセットの実機確認

公式資料と実機記事から形式は確定しているが、**課金に直結するので実際に 1 回は
自分の回線で確かめること。** 設定 → 書き換えルール → プリセット「楽天でんわ」を選ぶ。

- [ ] `003768` 付きで発信され、実際に接続される
- [ ] 明細で楽天でんわ経由の通話として課金されている

補足: 楽天でんわを使うと**家族間や同一キャリア間の無料通話が有料になる**。
該当する相手は「除外する番号」に登録して使うことを想定している。

### H-07. 公開直前の最終確認

- [ ] **配布する release APK を実機で動かす**（H-05 と同じ。未検証のまま配らない）
- [ ] APK に INTERNET 権限が無いことを確認
      `aapt2 dump permissions app-release.apk`
- [x] D-19（コミットのメールアドレス）を決める
- [x] D-20（署名鍵の方針）を決める
- [x] プライバシーポリシーの公開 URL を決める（`PRIVACY.md` をそのまま使用）

**公開完了: 2026-08-26** https://github.com/tmlksu/prefix-dialer
Release `v1.0.0` に `prefix-dialer-1.0.0.apk` を添付済み。

### H-02. ロール喪失時の挙動

他の通話リダイレクトアプリを入れて `ROLE_CALL_REDIRECTION` を奪わせ、
本アプリが警告を出すことを確認。

- [ ] ロールを失った状態でアプリを開くと警告が出る
- [ ] 再取得の導線が機能する

### H-03. ローミング中の挙動

海外に出ないと実地確認できないため、**実質はコードレビューのみ**。
テスト用のフラグを用意して、ローミング状態を擬似的に再現できるようにします。

### H-04. One UI 固有の確認

- [x] 通話履歴が元番号へ巻き戻ることを確認（2026-08-26）
- [x] `117`（時報）にプレフィックスが付かないことを実地で確認（2026-08-26）
- [ ] バッテリー最適化解除なしでも履歴書き換えが間に合うか
- [ ] Samsung Cloud の履歴同期との整合

**2026-08-26 に見つかった不具合（修正済み）:**
- 回線名が「3」「4」（購読 ID）で表示されていた。SIM 回線では
  `PhoneAccount.label` が空になるため。`SubscriptionManager` から取得するよう修正
- 「バッテリー最適化を解除する」ボタンで画面が遷移しなかった。
  `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` は
  `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 権限の宣言が必須で、未宣言だと無反応になる。
  この権限は Play の制限対象なので宣言せず、設定画面へ誘導する方式に変更

### H-08. targetSdk 36 ビルドの実機発信確認

1.1.0 で `targetSdk` を 35 → 36（Android 16）に上げた。ロールと発信経路はこのアプリの
中核なのに、`android.telecom` の API 35→36 差分ページが取得できず
「変更なし」と断定できていない（PLAY-RELEASE.md §1-(b)）。**H-05 と同じ理由で、
上げたビルドでの発信確認をやり直す必要がある。実発信は作者にしかできない。**

配布物: `./gradlew assembleGithubRelease` →
`app/build/outputs/apk/github/release/app-github-release.apk`（署名済み、`versionCode = 2`）。
1.0.0 と同じ鍵・同じ applicationId なので、そのまま上書き更新できる
（アンインストール不要。設定と発信記録は残る）。

**緊急通報番号には絶対に発信しないでください。**
`110` / `118` / `119` / `112` / `#7119` / `#8000` / `#9110` / `#8103` は対象外です。
確認に使えるのは `117`（時報）程度で、これも必要最小限に。

- [ ] 通常の携帯番号への発信でプレフィックスが付く（1.1.0 / targetSdk 36）
- [ ] `117`（時報）に付かない
- [ ] インストール・起動・設定画面の表示
- [ ] 詳細設定の末尾「このアプリについて」から、プライバシーポリシーと
      ソースコードがブラウザで開く（Play の要件そのものなので必ず見ること）
- [ ] **edge-to-edge が強制になったことで、画面の末尾がナビゲーションバーに
      潜っていない**（targetSdk 36 の挙動変更。オプトアウトはもう効かない）
- [ ] 戻る操作で設定画面からホームに戻る（予測バックへの移行の確認）
- [x] Rakuten Hand（Android 11）に play 版 1.1.0 をインストールし、起動・ロール付与・
      設定画面・「このアプリについて」のリンクを確認（2026-09-18、agent が adb で確認）

**上のチェック済み項目は S25 の代わりにはならない。** Rakuten Hand は Android 11（SDK 30）で、
`targetSdk 36` を宣言していても**端末側が 36 の挙動変更を実装していない**。
edge-to-edge の強制も予測バックも、この端末では再現しない。**S25 での確認は未了のまま。**

**2026-09-18 に Rakuten Hand（`a16282bf` / P780 / Android 11 / 720×1520）で確認できたこと:**

- `adb install -r` が成功。play 版なので通話履歴の権限は要求されない
  （requested permissions は `READ_PHONE_STATE` と内部権限の 2 つだけ）
- `versionCode=2` / `versionName=1.1.0` / `targetSdk=36` を `dumpsys package` で実測
- 一連の画面操作で `AndroidRuntime:E` のクラッシュはゼロ
- 通話リダイレクトのロール付与ダイアログが出て、許可すると警告カードが消える
- 「このアプリについて」→「プライバシーポリシー」のタップで Chrome が起動する
  （`ACTION_VIEW` の導線が実際に動くこと自体は確認できた）
- ストア用スクリーンショット 6 枚を撮影 → `store/screenshots/`

**あわせて見つかった要対応:** リンク先の
`https://tmlksu.github.io/prefix-dialer/privacy.html` は **404**（ホストからの `curl` でも 404）。
Pages が未有効なのではなく、**`docs/` を含むコミットがまだ push されていない**
（`origin/main` が `6873bab` 止まり）。push と Settings → Pages の有効化は作者の作業で、
これが済むまでアプリ内リンクも Play Console に登録する URL も 404 のまま。

**端末に残した状態:** play 版 1.1.0 がインストールされたまま、`ROLE_CALL_REDIRECTION` を
保持したまま、プリセット G-Call（`0063`）でマスタースイッチ ON。
**この端末に SIM を入れて発信すると `0063` が付く。** 予備機として SIM を入れる予定があるなら、
先にアンインストールするかマスタースイッチを OFF にしてください。

---

## 決定済み（記録）

| # | 内容 | 決定 |
|---|---|---|
| — | 配布形態 | 基本機能は権限ゼロ / 履歴書き換えはオプトイン。GitHub Releases と Play の両方の道を残す |
| — | ルールモデル | 型付きルールリスト。条件（種別 or 先頭一致）× 動作（`Apply` / `PassThrough`） |
| — | 先頭0の扱い | `KEEP` / `STRIP` / `TO_COUNTRY_CODE` を **ルール単位** で保持 |
| — | `suffix` | モデルにのみ保持し、1.0 では UI に出さない |
| — | UI スタック | Jetpack Compose へ移行 |
| — | 緊急通報の保護 | 名簿ではなく構造で守る（3桁以下を全ブロック / `#`・`*` を全ブロック） |
| — | テスト | 実発信を伴うテストは書かない。純 JVM のユニットテストで網羅 |
| — | JSON | 依存を増やさず手書きのシリアライザ（`org.json` はユニットテストでスタブ化されるため不可） |
| D-18 | 回線の識別 | `PhoneAccountHandle.id` を鍵にする。`onPlaceCall` が渡す値そのものなので判定経路に権限が不要。`READ_PHONE_STATE` は設定画面で回線名を表示するときだけ要求する |
| P-01 | 自動バックアップ | allowBackup は維持し、発信記録の prefs だけ除外（fullBackupContent + dataExtractionRules）。設定は機種変で引き継ぐ。BackupRulesTest で固定 |
| — | 既定ロケール | 既定を英語、`values-ja` を日本語。日本以外のロケールでは日本語ではなく英語にフォールバックさせる |
| — | プリセット名 | 永続化される `RuleSet.name` はロケール非依存の固定文字列。表示名は UI 層が解決する |
| — | R8 | release で有効。libphonenumber と Service に keep ルール。**実機動作は H-05 で要確認** |
