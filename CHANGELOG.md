# Changelog

## 1.1.0 — 未リリース

Google Play への提出準備。GitHub 版の機能は 1.0.0 から変わっていない。

### 変更

- **targetSdk / compileSdk を 36（Android 16）へ** — Play は 2026-08-31 以降、
  新規アプリの提出に API 36 以上を要求する。あわせて AGP 8.9.3 / Gradle 8.11.1 へ。
  Kotlin と Compose は据え置き
- **発信記録を Android の自動バックアップから除外** — 発信した番号そのものが
  Google ドライブへ送られていた。設定は引き継ぎの価値があるので対象に残す
- **アプリ内にプライバシーポリシーへの導線を追加** — 詳細設定の末尾に
  「このアプリについて」を新設し、バージョン・プライバシーポリシー・ソースコードを並べた
- **Play 版に機能差の説明を追加** — 通話履歴のセクションが出ない場所に、
  この版には書き換え機能が無いことと、その理由を 1 行で出す

### Play 版について

Google Play で配布する版には**通話履歴の書き換え機能がない**。
Play は既定の電話アプリでないアプリが通話履歴の権限を宣言すること自体を認めていないため、
権限ごと外してある。プレフィックスを付ける基本機能はこの権限を必要としないので影響しない。
この機能が要る場合は GitHub Releases の版を使うこと。

### 動作確認

- ユニットテスト 118 件（両フレーバー。実発信を伴うテストは含まない）
- **targetSdk 36 での実発信は未確認**（DECISIONS.md の H-08）

## 1.0.0 — 2026-08-26

初回リリース。

### 主な機能

- **番号種別ごとの書き換えルール** — 携帯 / 固定 / IP電話それぞれに、プレフィックスと
  元番号の先頭 `0` の扱い（残す / 取る / `81` にする）を個別に設定できる。
  事業者によっては携帯向けと固定向けで形式が違うため、種別ごとに持てるようにしている
- **事業者プリセット** — G-Call、楽天でんわ。どちらも公開資料で形式を確認したもののみ収録
- **緊急通報のハードガード** — 緊急通報・特番・`#` 系ダイヤルは、**どんな設定でも書き換わらない**
- **回線（SIM）ごとの ON/OFF** — 契約していない回線での課金事故を避けるため
- **ローミング中の自動停止**（既定で有効）
- **番号単位の除外リスト** — 「この番号だけは付けない」
- **発信記録** — 各発信で何をしたか、付かなかった場合はその理由が残る
- **通話履歴の書き換え**（オプトイン・既定 OFF） — 発信後に履歴の番号を元に戻す
- **設定の JSON エクスポート / インポート**
- 日本語 / 英語

### プライバシー

インターネット権限を持たないため、情報を外部へ送信できない。詳細は
[PRIVACY.md](PRIVACY.md)。配布 APK は `aapt2 dump permissions` で検証できる。

### 権限

プレフィックスを付ける基本機能は、通話履歴の権限を**一切必要としない**。
通話履歴・連絡先の権限は「通話履歴の書き換え」を有効にした瞬間に初めて要求する。

### 動作確認

- Samsung Galaxy S25 / One UI 7 / Android 15 — R8 難読化した release ビルドで
  発信時のプレフィックス付与と設定画面の動作を確認
- ユニットテスト 116 件（実発信を伴うテストは含まない）
- 3 桁番号 `000`〜`999` の総当たりで、特番が書き換わらないことを検証

### インストール時の注意

自分でビルドした debug 版が入っている端末には、そのままでは上書きできない。
署名鍵が違うため Android が更新を拒否する。debug 版をアンインストールしてから
インストールすること（設定は消える）。

### 既知の制約

- 通話リダイレクトアプリは端末に 1 つだけ。他の番号書き換え／着信拒否系アプリとは排他。
  他アプリに役割を奪われた場合はアプリ起動時に警告する
- 履歴の書き換えを使う場合、Samsung 端末ではバッテリー最適化の解除がほぼ必須
- 国際発信（`010`）は対象外。今後の課題
- 番号にポーズ（`,` `;`）が含まれる場合は書き換えず、そのまま発信する
- 現時点で実機確認できているのは Samsung Galaxy S25 のみ
