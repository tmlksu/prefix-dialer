# Roadmap

公開アプリ化に向けた作業メモ。

## 現状 (2026-08-24)

- ✅ 発信時に国内の携帯/固定/IP電話へ自動で `0063` プレフィックスを付与（`CallRedirectionService`）
- ✅ 発信後に `CallLog` を元番号へ書き戻し、電話帳マッチを維持（短命フォアグラウンドサービス）
- ✅ 除外条件（`0120`/`0800`/`0570`/`0990`/`+81`以外/`010`/二重付与）を実装
- ✅ **緊急通報・特番のハードガード**（`ProtectedNumbers`）— ユーザー設定より上位の安全層
- ✅ ユニットテスト 32 件（うち緊急通報の不変条件テスト 13 件）
- ✅ Samsung Galaxy S25 / One UI 7 / Android 15 実機で発信〜履歴書き換えまで動作確認
- MVP の applicationId は `com.example.prefixdialer` のまま（公開前に要変更）

## 1.0 スコープ（確定）

### A. 設定の基盤 ★最優先・手戻りが最大の箇所

「機能ON/OFF」「プレフィックス設定」「番号種別ごとの書き換え」「プリセット」は
すべて**同一のルールモデル**に載る。ここを最初に固めてから UI を作る。

- [x] **ルールモデルの定義** — `DialRule.kt`。条件（番号種別 or 先頭一致パターン）× 動作
      （`Apply(prefix, leadingZero, suffix)` or `PassThrough`）。上から評価し最初にマッチしたものを適用
- [x] **先頭0の扱いを3値で持つ** — `LeadingZero.KEEP` / `STRIP` / `TO_COUNTRY_CODE`。
      **ルール単位**で保持する（同じ事業者でも携帯向けと固定向けで扱いが違う場合があるため、
      事業者単位でもプリセット単位でもなく、ルール単位である必要がある）
- [x] **評価器** — `RuleEngine.kt`。安全層 → マスタースイッチ → 二重付与除外 → 種別除外 → ルール評価
- [x] **マスター ON/OFF** — `RuleSet.enabled`。モデル側は完了、UI 接続は未了
- [x] **プリセットの器** — `Presets.kt`。**出典を確認できたものだけ収録**する方針を KDoc に明記。
      現在の収録は G-Call（実機確認済み）と空テンプレートの「カスタム」のみ
- [ ] **設定の永続化** — `PhoneNumberPrefixer.activeRuleSet` を SharedPreferences 等から読むよう差し替え
- [ ] **設定 UI（Compose）** — 種別ごとの行を編集する形。プレフィックスは単一テキスト欄にはできない
- [ ] **設定のエクスポート/インポート (JSON)** — 機種変・バックアップ用。
      依存を増やさず済むよう手書きのシリアライザを想定（Android の `org.json` は
      ユニットテストでスタブになるため使わない）

### B. 安全性

- [x] **緊急通報・特番のハードガード** — `ProtectedNumbers`。実装済み。
      名簿方式ではなく構造で守る（3桁以下は全ブロック、`#`/`*` を含む番号は全ブロック）。
      **どんなユーザールールでもバイパス不可**。詳細は `ProtectedNumbers` の KDoc
- [ ] **ローミング中の自動 OFF** — 海外で prefix は無意味な上に課金事故になる。
      `TelephonyManager.isNetworkRoaming()` で判定
- [ ] **SIM / 回線ごとの適用制御** — 現状 `onPlaceCall` の `initialPhoneAccount` を捨てている。
      デュアル SIM で契約していない回線に付けると課金事故

### C. 履歴書き換え（CALL_LOG）のオプトイン化

Play ポリシー対応であると同時に、ユーザー保護でもある。
CALL_LOG 書き込み権限を持つアプリは履歴を全消しできてしまうため、
「何をするアプリか分からないまま権限を渡す」状態を作らない。

- [ ] **基本機能を CALL_LOG 権限ゼロで動作させる** — プレフィックス付与だけなら権限不要
- [ ] **履歴書き換えを独立した ON/OFF に分離** — デフォルト OFF
- [ ] **権限は機能を有効化した瞬間に要求**（インストール直後には要求しない）
- [ ] 機能の説明画面 — 何のために履歴を読み書きするのかを明示

### D. 信頼性・可観測性

- [ ] **ロール喪失の検知** — `ROLE_CALL_REDIRECTION` は端末に 1 アプリのみ。他アプリに奪われると
      **無言でプレフィックスが付かなくなり、ユーザーは気づかず通常料金を払い続ける**。
      `onResume` で検知して警告表示 + 再取得導線
- [ ] **アプリ内の発信記録** — 直近 N 件の「元番号 → 書き換え後」「対象外だった場合はその理由」。
      自前の記録なので権限不要。ユーザーが効果を検証でき、問い合わせの切り分けにも効く
- [ ] **番号単位の除外リスト** — 「この番号だけは絶対に付けない」（会社の内線・特定の相手など）

### E. リリース作業（ブロッカー）

- [ ] **applicationId のリネーム** — `com.example.*` は Play Console で拒否される。
      パッケージ名も合わせて変更
- [ ] **署名設定** — release 用 keystore を作成し `signingConfigs` を設定。
      keystore はリポジトリに含めない（`keystore.properties` 方式）
- [ ] **プライバシーポリシー** — URL を Play Console に登録
- [ ] アプリアイコン（現状は端末標準アイコンを流用）
- [ ] 多言語対応（ja / en）

## テスト方針

**実発信を伴う自動テストは行わない。**

- 判定ロジック（`PhoneNumberPrefixer` / `ProtectedNumbers`）は Android 非依存の純粋関数として
  切り出し、ローカルユニットテストで網羅する。テスト中に発信は一切発生しない
- `ProtectedNumbersTest` は**恒久的な安全網**。ルール追加・プレフィックス変更のたびに走り、
  1 件でも特番が書き換わったら失敗する。ここが赤くなったら期待値ではなく実装を直す
- 3 桁番号は `000`〜`999` を総当たりで検証（1XY 帯の取りこぼし防止）
- **実機での手動確認に緊急通報番号を使わない** — `110` / `118` / `119` / `112` および
  `#7119` / `#8000` / `#9110` / `#8103` には絶対に発信しない。
  実害なく確認できるのは `117`（時報）程度で、これも必要最小限に留める
- [ ] CI (GitHub Actions) — `test` + `lint` + `assembleDebug`

## パーキングロット（1.0 リリース前に再検討）

優先度を下げたもの。1.0 の本体機能が固まってから、リリース前に取捨選択する。

- 発信前の確認ダイアログ（`onPlaceCall` の `allowInteractiveResponse` を利用）
- 国際発信のプレフィックス対応（現状 `010` はハードコードで除外）
- DTMF ポーズ付き番号への対応（現状は保護＝素通し。`ProtectedNumbers` の該当箇所を参照）
- 履歴書き換えの信頼性向上（One UI の表示キャッシュ・Samsung Cloud 同期との整合）
- Samsung 以外の端末での実機確認
- **Google アカウント同期による設定バックアップ** — まず JSON エクスポート/インポートで
  実用は満たせるため、1.0 後の課題とする

## 未確定の論点（作業を進めながら判断）

- **配布形態** — 現状は「基本機能は権限ゼロ、履歴書き換えはオプトイン」の設計にすることで、
  GitHub Releases 先行配布と Play 申請の**どちらの道も残す**方針で進める
- **UI スタック** — 設定画面がリスト中心に増えるため Jetpack Compose への移行を想定。
  現状は手書き `LinearLayout` 154 行なので移行コストは小さいうちに払う
- **applicationId のドメイン** — 未定。所有ドメインが無ければ `io.github.<user>.prefixdialer`
- **ライセンス** — 現状 MIT（`LICENSE`）。公開前に最終確認
